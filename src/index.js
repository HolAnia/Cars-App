import React from 'react';
import ReactDOM from 'react-dom';
import CarsApp from './App';

ReactDOM.render(
  <React.StrictMode>
    <CarsApp />
  </React.StrictMode>,
  document.getElementById('root')
);

